<div class="card book">
  <img class="card-img-top" src="{{ book.cover }}" alt="{{ book.title }}">
  <div class="card-body">
    <h5 class="card-title">
      <em>{{ book.title }}</em> <small>({{ book.year }})</small>
    </h5>
    <p class="card-text">{{ book.author }}</p>
  </div>
</div>

